web: python manage.py migrate && celery -A documind worker --loglevel=info --pool=solo --detach --logfile=/tmp/celery.log && gunicorn documind.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120
